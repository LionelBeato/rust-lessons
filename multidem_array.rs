fn main() {
    
    // dimensions must be the same shape (type and size)! 
    let parking_lot = [
        [1,2,3],
        [4,5,6]
    ]; 
    
    let number = parking_lot[0][1]; 
    println!("number is {}", number); 

    // three dimenionsal array! 
    let garage = [[[0;100]20]5]; 
}